import { app } from "../../../scripts/app.js";

/*app.registerExtension({
	name: "cg.custom.multipick",
    async nodeCreated(node) {
        node?.widgets?.forEach(w => {
            if (w) {
                console.log(w);
            }
        });
    }
});*/

